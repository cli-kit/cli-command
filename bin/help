#!/usr/bin/env node

var path = require('path');
require('ttycolor')().defaults();
var pkg = path.normalize(path.join(__dirname, '..', 'package.json'));
var cli = require('..')(pkg);
cli
  .version()
  .help()
  .command('mock', 'a mock command')
  .parse();
